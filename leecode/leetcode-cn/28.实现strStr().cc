class Solution {
public:
    int strStr(string haystack, string needle) {

    }

    void fillNext(string& needle, vector<int>& next) {
        assert(needle.length() == next.size() && needle.length() >= 2);
        
    }
};